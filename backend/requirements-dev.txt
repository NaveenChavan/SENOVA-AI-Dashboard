# Development-only dependencies (test suite). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.4
# TestClient in tests/test_api.py needs httpx; FastAPI does not require it at runtime.
httpx==0.28.1
