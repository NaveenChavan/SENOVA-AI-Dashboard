# SENOVA AI Dashboard environment variables.
# Supabase integration has been removed — uploaded files are processed
# directly from local disk. Add only the variables you still need here
# (e.g. CORS_ORIGINS, GOOGLE_CLIENT_ID, MAX_UPLOAD_SIZE_MB).
