[pytest]
# Tests import the app as ``app.*``, so the backend directory must be on the
# path. ``testpaths`` keeps a bare ``pytest`` run scoped to the suite.
pythonpath = .
testpaths = tests
addopts = -q
